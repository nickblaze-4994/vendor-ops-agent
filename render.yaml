services:
  - type: web
    name: vendor-ops-agent
    runtime: node
    plan: free
    buildCommand: npm install && npm --prefix web install && npm --prefix web run build
    startCommand: node server/index.js
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: ANTHROPIC_API_KEY
        sync: false
